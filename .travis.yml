sudo: required

services: docker

before_install:
  - docker-compose up --build -d

script:
  - docker-compose exex --env 'RAILS_ENV=test' web rails db:create
  - docker-compose exex --env 'RAILS_ENV=test' web rails db:migrate
  - docker-compose exex --env 'RAILS_ENV=test' web rails test